<%= form_for(@agentparam) do |f| %>
  <% if @agentparam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@agentparam.errors.count, "error") %> prohibited this agentparam from being saved:</h2>

      <ul>
      <% @agentparam.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :infectious_dose %><br>
    <%= f.text_field :infectious_dose %>
  </div>
  <div class="field">
    <%= f.label :level_of_infectious_dose %><br>
    <%= f.text_field :level_of_infectious_dose %>
  </div>
  <div class="field">
    <%= f.label :infected_species %><br>
    <%= f.text_field :infected_species %>
  </div>
  <div class="field">
    <%= f.label :agent_id %><br>
    <%= f.number_field :agent_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
